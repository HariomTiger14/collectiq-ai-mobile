# Bootstrap Runtime Comparison

Reference status: no direct approved bootstrap/splash image found.

Current runtime evidence:

- current_runtime/fresh_launch_first_observable.png
- current_runtime/fresh_launch_first_observable.xml

Comparison result: first observable launch state reaches onboarding welcome after app data clear. Bootstrap/splash transient was not captured as a separate approved state.
