# Functional validation

The three S01 actions remain present, enabled and tappable. Sample flow is verified. Automated camera/gallery behavior and active-session handoff pass. Native camera/gallery E2E is deliberately deferred and does not block visual approval; see `deferred_native_flow_validation.md`.

Camera, gallery and sample callbacks remain wired to unchanged controller methods. Notification remains injectable.
