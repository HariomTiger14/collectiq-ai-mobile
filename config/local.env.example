# Copy this file to config/local.env for machine-specific local overrides.
# Do not commit config/local.env.

# Optional. Android phones need a LAN-reachable backend URL, not 127.0.0.1.
# API_BASE_URL=http://192.168.0.81:8000
